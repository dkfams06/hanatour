# 🌐 한아투어 환경 변수 설정 가이드
# 이 파일을 복사하여 .env.local 파일을 생성하고 실제 값으로 변경해주세요.

# ===========================================
# 📊 데이터베이스 설정 (MariaDB/MySQL)
# ===========================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=hanatour_user
DB_PASSWORD=your_secure_password_here
DB_NAME=hanatour_db

# ===========================================
# 🔐 JWT 보안 설정
# ===========================================
# 강력한 랜덤 문자열을 사용하세요 (최소 32자 이상 권장)
# 예: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here_at_least_32_characters

# ===========================================
# 📧 이메일 서비스 설정 (Gmail SMTP)
# ===========================================
# Gmail 계정과 앱 비밀번호를 사용
# 2단계 인증 활성화 후 앱 비밀번호 생성 필요
EMAIL_USER=your_gmail@gmail.com
EMAIL_PASS=your_16_digit_app_password

# ===========================================
# 🌐 애플리케이션 설정
# ===========================================
# 실제 도메인 URL로 변경 (끝에 슬래시 없이)
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# 운영 환경에서는 production으로 설정
NODE_ENV=production

# ===========================================
# 📝 설정 가이드
# ===========================================

# 1. 데이터베이스 설정:
#    - MariaDB 또는 MySQL 서버가 실행 중이어야 합니다
#    - hanatour_user 계정과 hanatour_db 데이터베이스를 미리 생성해주세요
#    - 제공된 SQL 파일들을 순서대로 실행하여 테이블을 생성해주세요

# 2. JWT 보안키 생성:
#    터미널에서 다음 명령어로 안전한 키를 생성할 수 있습니다:
#    openssl rand -base64 32

# 3. Gmail SMTP 설정:
#    - Google 계정에서 2단계 인증 활성화
#    - Google 계정 관리 > 보안 > 앱 비밀번호에서 새 앱 비밀번호 생성
#    - 생성된 16자리 비밀번호를 EMAIL_PASS에 입력

# 4. 파일 위치:
#    이 파일을 .env.local로 복사하고 위 값들을 실제 값으로 변경하세요:
#    cp env.example .env.local

# 5. 보안 주의사항:
#    - .env.local 파일은 절대 Git에 커밋하지 마세요
#    - 운영 서버에서는 더욱 강력한 비밀번호를 사용하세요
#    - 정기적으로 JWT_SECRET을 변경하는 것을 권장합니다
